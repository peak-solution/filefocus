# Custom CSV Plugin

Provide a custom CSV reader for CSV files.

The example has:
* first line: column name
* second line: column units
* third line: ignored
* fourth line onwards: actual data

Check `not_my_file` and `data` for adjustments to your format.

Restart custom csv container after code changes.
To adjust extension change PODS settings and restart container.